import 'package:flutter/material.dart';

class BodyHelp extends StatelessWidget {
  const BodyHelp({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Get help from here!"),
    );
  }
}
